import test from 'node:test';import assert from 'node:assert/strict';import{analyzeResume,terms}from'../public/assets/ats-core.js';
const resume=`Jane Example\njane@example.com | +91 9876543210 | github.com/jane\nSummary\nSoftware developer focused on accessible web applications and reliable automation.\nEducation\nBachelor of Technology 2020\nExperience\n• Built a reporting dashboard for 100 users with JavaScript and Python.\n• Reduced processing time by 30% through automated data validation.\n• Designed customer workflows and implemented accessible navigation across applications.\n• Led delivery of 5 projects with clear documentation and careful testing.\nSkills\nPython JavaScript HTML CSS SQL Git`;
test('score is deterministic, bounded and adds up to the visible breakdown',()=>{const r=analyzeResume(resume);assert.equal(r.score,r.categories.reduce((s,c)=>s+c.earned,0));assert.ok(r.score>=0&&r.score<=100);assert.deepEqual(r,analyzeResume(resume));assert.match(r.warnings.at(-1),/not a score from an employer/)});
test('blank and scanned resume text is rejected rather than scored',()=>{assert.throws(()=>analyzeResume(''));assert.throws(()=>analyzeResume('Page 1 image'),/Not enough readable/)});
test('keyword coverage is separate and exact, with no substring matches',()=>{const r=analyzeResume(resume,{job:'Python Rust JavaScript'});assert.deepEqual(r.jobMatch.matched,['python','javascript']);assert.deepEqual(r.jobMatch.missing,['rust']);assert.equal(r.jobMatch.score,67);assert.equal(r.score,analyzeResume(resume).score);assert.ok(!terms('javascript').includes('java'))});
test('missing contact data lowers the contact category',()=>{const r=analyzeResume(resume.replace('jane@example.com | +91 9876543210 | github.com/jane',''));assert.equal(r.categories.find(c=>c.name==='Contact details').earned,0)});
